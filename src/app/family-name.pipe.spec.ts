import { FamilyNamePipe } from './family-name.pipe';

describe('FamilyNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FamilyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
